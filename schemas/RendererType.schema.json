{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "RendererType.schema.json",
  "title": "RendererType",
  "description": "The type of renderer used to display the view.\n\nParsed from `\"viewRenderer\"` or `\"view.renderer\"` keys in the JSON configuration.\nDefaults to `RendererType/native` if the value is missing or unrecognized.",
  "type": "string",
  "enum": [
    "native",
    "html",
    "custom",
    "sheet"
  ]
}
